# 1 тапсырма
class City:
    def sushestvuet(self):
        print("Город существует")

class Road:
    def __init__(self):
        self.city = City()

    def open(self):
        self.city.sushestvuet()
            print("Город существует(1/3)")

class Building:
    def __init__(self):
        self.city = City()

    def ready(self):
        self.city.sushestvuer()
            print("Город существует(2/3)")

class Park:
    def __init__(self):
        self.city = City()
 
    def Ready(self):
        self.city.sushestvuet()
            print("Город существует(3/3)")
#2 тапсырма

City.sushestvuer()
Road.open()
Building.ready()
Park.Ready()

from abc import ABC, absrtactmethod

class City:
    def __init__(self, city_id, sushestvuet):
        self.city_id = city_id
        self.sushestvuet = sushestvuet

class Infrastructure(ABC):
    @abstractmethod
    def on(self, est: str):
        pass

class Road(Infrastructure):
    def on(self, est: str):
        print(f"Есть {est}")

class Building(Infrasstructure):
    def on(self, est: str):
        print(f"Есть {est}")

class Park(Infrastrcture):
    def on(self, est: str):
        print(f"Есть {est}")

class InfrastructureService:
    def __init__(self.cityu.City)
        self.cityu = cityu

    def citu(self, est: str):
        self.cityu.on(est)

city = City(city_id = 123, amount = "Караганды")

road = InfrastructureService(Road())
road.citu(f"City{city.city_id}")

building = InfrastructureService(Building())
building.citu(f"City{city.city_id}")

park = InfrastructureService(Park())
park.citu(f"City{city.city_id}")


